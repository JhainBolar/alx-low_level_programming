#include "main.h"
/**
 * _isupper - Checks if c is an upper case
 *  @c: input character
 *  Return: Returns 1 if c is upper otherwise 0
 */

int _isupper(int c)
{
	if(c >= 'A' && c <= 'Z')
	{
		return 1;
	}

       	else 

	{
		return 0;
	}
}
