#include 'main.h'

/**
 * 3-islower.c - Check if alphabet is uppercase or lowecase
 *
 * Return: Returns void.
 */
int _islower(int c)
{
	if (c <= 'a' && c >= 'z')

	{
		return (0);
	
	}

	else

	{

		return (1);

	}
}
