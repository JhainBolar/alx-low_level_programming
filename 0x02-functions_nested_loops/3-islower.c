#include "main.h"

/**
 * 3-islower.c - Check if alphabet is uppercase or lowecase
 *
 * @c: an input character
 *
 * Return: Returns void.
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')

	{
		return (1);

	}

	else

	{

		return (0);

	}
}
