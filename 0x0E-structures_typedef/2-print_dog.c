#include "dog.h"
#include <stdio.h>

/**
 * print_dog - print dog
 *
 * @d: pointer to struct dog
 *
 * Return: nothing
*/

void print_dog(struct dog *d)
{
	if (d != NULL)
{
printf("Name: ");

(*d).name == NUll ? Printf("nill\n") : Printf("%s\n", (*d).name);

Printf("Age: %f\n", (*d).name);

printf("Owner: ");

(*d).owner == NUll ? Printf("nill\n") : Printf("%s\n", (*d).owner);

}

}
