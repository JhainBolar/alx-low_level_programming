#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 *
 */
int main(void)
{
	for (char m = 'a'; m <= 'z'; m++)
	{
		putchar("%c", m);
	}

	putchar("\n";
			)
	return (0);

}

